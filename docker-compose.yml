services:
  relay:
    build: .
    container_name: relay
    ports:
      - "8080:8080"
    volumes:
      - relay-data:/data
    environment:
      - DB_PATH=/data/relay.db
      - PORT=8080
    restart: unless-stopped

volumes:
  relay-data:
